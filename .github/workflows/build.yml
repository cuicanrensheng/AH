name: Android Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Install ImageMagick
        run: sudo apt-get install -y imagemagick

      - name: Auto create default icon folders
        run: |
          mkdir -p app/src/main/res/mipmap-mdpi
          mkdir -p app/src/main/res/mipmap-hdpi
          mkdir -p app/src/main/res/mipmap-xhdpi
          mkdir -p app/src/main/res/mipmap-xxhdpi
          mkdir -p app/src/main/res/mipmap-xxxhdpi
          mkdir -p app/src/main/res/mipmap-anydpi-v26
          mkdir -p app/src/main/res/values

      - name: Auto generate default ic_launcher png
        run: |
          convert -size 48x48 xc:#cccccc app/src/main/res/mipmap-mdpi/ic_launcher.png
          convert -size 72x72 xc:#cccccc app/src/main/res/mipmap-hdpi/ic_launcher.png
          convert -size 96x96 xc:#cccccc app/src/main/res/mipmap-xhdpi/ic_launcher.png
          convert -size 144x144 xc:#cccccc app/src/main/res/mipmap-xxhdpi/ic_launcher.png
          convert -size 192x192 xc:#cccccc app/src/main/res/mipmap-xxxhdpi/ic_launcher.png

      - name: Create adaptive icon xml
        run: |
          cat > app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
              <background android:drawable="@color/white"/>
          </adaptive-icon>
          EOF

          cat > app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
              <background android:drawable="@color/white"/>
          </adaptive-icon>
          EOF

      - name: Create colors.xml
        run: |
          cat > app/src/main/res/values/colors.xml << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <color name="white">#FFFFFFFF</color>
          </resources>
          EOF

      - name: Grant execute permission
        run: chmod +x gradlew

      # 关键修复：先创建目录，防止 keytool 报错
      - name: Prepare build dir
        run: mkdir -p app/build

      # 生成密钥库，路径和密码与 build.gradle 完全对齐
      - name: Auto Generate Release Keystore
        run: |
          keytool -genkey -noprompt \
          -keystore app/build/auto-release.jks \
          -alias auto \
          -keyalg RSA -keysize 2048 -validity 10000 \
          -storepass auto1234 \
          -keypass auto1234 \
          -dname "CN=Auto,OU=Dev,O=Dev,L=City,S=State,C=CN"

      # 构建时传入和 build.gradle 完全匹配的环境变量
      - name: Clean & Build Release Auto Sign
        run: ./gradlew clean assembleRelease
        env:
          KS_FILE: build/auto-release.jks
          KS_PASS: auto1234
          KS_ALIAS: auto
          KEY_PASS: auto1234

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: Release-APK
          path: app/build/outputs/apk/release/*.apk
