buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.7.0'
        // 补充 Kotlin 插件依赖，确保能被找到
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.0"
    }
}
