pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

// 去掉 dependencyResolutionManagement，改用兼容写法
rootProject.name = "TVLive"
include ':app'
