# 开发会话记录（2026-08-25）

## 一、任务背景

用户要求：检查所有虎牙频道，验证"清晰度设置"对接 SDK 清晰度接口是否有问题。

**今日涉及 33 个源码文件修改**，分为五大类：**功能修复 / SDK 兼容 / 性能优化 / 直播源与 EPG / 减小体积**。

---

## 二、减小体积（专项，APK 10.6MB）

### 2.1 build.gradle（11:32 修改）

```gradle
// release 构建
minifyEnabled true
shrinkResources false → true          // 启用资源压缩，剔除未使用资源
useLegacyPackaging false → true       // SO 以 deflate 压缩进 APK（安装时由系统解压）
```

- **效果**：release arm64-v8a APK 从约 24MB → **10.6MB**（armeabi-v7a 9.4MB）
- 原方案 `useLegacyPackaging=false`：SO 未压缩（STORED）存储 → APK 更大（24MB），但安装后不额外产生 lib/ 解压体积；改 `true` 后 APK 显著变小

**重要警告注释（勿踩坑）**：
- ⚠️ **勿剔除 `libmarsxlog.so` / `libstlport_shared.so`**（xlog 链路）：剔除后 `mars.Mars.loadDefaultMarsLibrary` 加载失败 → 整个 mars 网络栈（StnLogic/HySignal/MtpMarsTransporter）不可用 → 取流失败
- ⚠️ **`libc++_shared.so` 不可剔除**（libmmkv.so + libmarsstn.so 的 DT_NEEDED 硬依赖）
- 这两条链是 SDK init 的强制加载项（ArkValue.initLog → Xlog.<clinit>）

### 2.2 删除 assets/js 源文件

- 删除 `app/src/main/assets/js/` 下的 `huya_parse.js` / `native_layer.js` / `yt_source_root.js`（不再打包进 APK）
- `app/src/main/assets/` 现仅保留 `live_source_3.txt`（直播源3）

### 2.3 APK 产物（今日构建）

| 变体 | 大小 | 时间 |
|------|------|------|
| release arm64-v8a | **10.6MB** | 12:40 |
| release armeabi-v7a | 9.4MB | 12:40 |
| debug arm64-v8a | 19.2MB | 13:58 |

---

## 三、虎牙 SDK 清晰度对接（13:40-13:57，今日核心）

### 3.1 问题一：UI 清晰度标签未对接 SDK 清晰度名

**描述**：`Variant.fromHuyaStreamInfo` 用 URL 码率推断标签（如 `_4000.` → "1080p"），忽略 SDK 的 `bitRateDisplayName`（"蓝光6M"/"蓝光4M"/"超清"/"流畅"）。

**修改**：`app/src/main/java/com/tv/live/util/Variant.java`

```java
// 🔴【对接SDK清晰度接口】优先使用 SDK 官方清晰度名作为 UI 标签
if (!TextUtils.isEmpty(s.bitRateDisplayName)) {
    v.huyaBitRateDisplayName = s.bitRateDisplayName;
    v.resolutionLabel = s.bitRateDisplayName;
    // height 仅用于按高度兜底匹配
    ...
}
```

**效果**：UI 档位标签与 SDK 完全一致。

### 3.2 问题二（核心）：切频道返回后清晰度档位递减（4档→3档→1档）

**现象**：播放房间 A（4档）→ 切到房间 B → 返回 A 变成 3档；多次切换后再返回变 1档。

**根因**（logcat 实证）：
```
13:50:11 doPlay 房间B (流URL)  → 无 variantList 填充
13:50:12 doPlay 房间A (流URL)  → 无 variantList 填充
```

**污染链条**：
1. 虎牙解析成功后 `currentChannel.setMainPlayUrl(defaultUrl)` 把 `huya://room/A` 覆盖成具体流 URL（`TVPlayerManager.java` 原1717行 / `HuyaStreamPlayer.java` 原347行）
2. Channel 对象缓存在 `channelSourceList`，污染永久生效
3. 下次 `playChannel → getPlayUrl()` 返回流 URL（或 `currentLineIndex` 残留返回 backupUrls 流 URL）→ 直接 doPlay → **不走 playHuyaStream → variantList 不重新填充** → 残留上一个房间档位

**修复（3 处）**：
1. `TVPlayerManager.java`：删除 `setMainPlayUrl(defaultUrl)`，mainPlayUrl 保持 `huya://room/xxx` 房间协议（线路 URL 仍写 backupUrls 供线路选择）
2. `HuyaStreamPlayer.java`：同样删除
3. `MainActivity.playChannel()`：虎牙房间切台前 `setCurrentLineIndex(0)` + `clearBackupUrls()`（仅虎牙房间，普通频道备用源不受影响）

**效果**：切走再切回任何虎牙频道必然重新 SDK 解析填充 variantList，档位始终为当前房间真实档位。用户确认"可以了，解决问题了"。

### 3.3 调查中排除的假设

- `fetchAndParseMasterPlaylistHuya`：死代码，无外部调用
- 预解析缓存路径不填充 variantList：日志证实缓存命中也会填充，排除
- 不同房间档位本就不同（8/6/2 变体 = 4/3/1 档）：属实但非根因
- 线路轮询逻辑（多次进入同一直播间自动切线路）：存在但仅切线路不改变档位数量

---

## 四、SDK 兼容修复（12:12-13:13）

### 4.1 MyApplication.java（13:13）

- **动态注册 receiver 补 `RECEIVER_NOT_EXPORTED`**：Android 14+（targetSdk 34+）强制动态 receiver 声明导出标志；虎牙 SDK `HuyaBerryImpl.init()` 裸调 `registerReceiver` 会抛 SecurityException。用反射 `IntentFilter.setReceiverFlags`（API 33+）统一补上 `RECEIVER_NOT_EXPORTED`（重写 Application 的 3 个 registerReceiver 重载）
- **反调试日志级别调整**：ERROR → WARN（避免误报到 Bugly；真正的篡改检测 Frida/Xposed 已在 AntiDebug 内部上报）
- **移除 `NetUtil.init()`**（不再需要的网络工具初始化）

### 4.2 SdkCompatHook.java（新文件，12:12）

SDK 兼容性钩子，配合启动修复。

---

## 五、直播源 / 订阅列表 UI 重构（08:56-09:08）

### 5.1 订阅列表 UI（SubscriptionAdapter + 资源文件）

- `SubscriptionAdapter.java`（353行重构）：列表行视觉 100% 跟随 Android 原生状态（`state_focused / state_selected / state_activated`），名称文本用双 TextView 叠加实现加粗/变色
- `item_subscription_list.xml`（73行）：名称容器重构（同位置同尺寸双文本）
- `subscription_row_content_bg.xml`：默认态改全透明，去除每行灰色矩形块，与对话框深蓝底色融合
- `subscription_action_text.xml` / `subscription_check_text.xml` / `subscription_row_text.xml`：状态颜色调整

### 5.2 SettingsDialog.java（130行）

- **直播源切换高亮修复**：`setSelectedPosition(currentDefault)` 调用时机修正（原先在 `setListView`/`setAdapter` 之前调用无效）；对话框 show + 200ms 后 `ensureActivatedImmediate()` 兜底，保证触摸模式下默认行蓝底/蓝字/加粗
- 直播源切换逻辑修正：用 `realPos` 处理"最新源列表"与实际列表的错位（`position >= latest.size()` 时回退）
- 切换直播源后同步更新 `UrlConfig.LIVE_URL / EPG_URL`
- 二维码生成改用 `AppExecutors.io()` 线程池

### 5.3 SourceManager.java（333行）

直播源管理重构（含多源列表、历史源管理、默认源切换逻辑）。

### 5.4 UrlConfig.java（62行）

- **内置源2地址迁移**：`LIVE_URL` 相关地址更新
- 支持切换时同步 `UrlConfig` 当前地址

### 5.5 AppCoreManager.java（139行）

- **修复"切换按钮点了没生效"**：网络加载返回 0 条频道时视为「源地址失效/404/返回空内容」→ **清空列表**而非 merge 保留旧缓存；仅当网络真正返回有效频道数才 merge
- 网络返回空频道数时额外回调 `onLiveSourceFailed`，让 MainActivity 弹 Toast 提示用户

### 5.6 ChannelPanelController.java

- **切源后重置分组状态为"全部"**：否则 `currentGroupName` 残留旧源组名导致频道列表显示错位

### 5.7 PlaylistParser.java（125行）

**M3U/TXT 双格式解析重构**：
- **格式 A：DIYP TXT**（666 源使用）：`央卫,#genre#` 分组标记 + `CCTV-1,http://xxx.m3u8` 频道条目 + 同名多源合并为备用源
- **格式 B：标准 M3U**：`#EXTINF` 状态机重写（`pendingM3uUri` 标记），tvg-id / group-title / 名称解析

---

## 六、日志 / 崩溃上报体系（09:08）

- `BuglyLogSender.java`（470行）、`ExceptionReporter.java`（338行）：Bugly 上报与异常上报体系重构
- `HuyaSDKLogger.java`（57行）、`LogCollector.java`（61行，新增分割线标记）、`LogServer.java`（19行）：日志收集完善
- `HuyaSDKParser.java`：`extractFullStreamList` / `fallbackExtractAsSingle` 各 catch 分支补充 `ExceptionReporter.report()` 异常埋点
- `log-monitor/`（日志监控服务端）：`server.js`（2054行）、`public/app.js`（2249行）、`index.html`、`style.css`、`package.json` 更新

---

## 七、安全 / 反调试（09:08）

- `AntiDebug.java`（132行）：反调试检测完善（Frida/Xposed 检测内部上报）
- `SecurityGuard.java`（29行）、`TamperReporter.java`（17行）：安全检查与篡改上报调整

---

## 八、EPG 节目单修复（09:08）

- `EpgManager.java`：**YYYYMMDD 精确整数日期键**（消除中文周几标签不一致问题）+ 时区偏移解析（`yyyyMMddHHmmss +0000`）+ 日期范围扩大到未来 7 天 + 清理一起看（移除 HuyaTogetherWatchManager 引用）
- `EpgManagerWrapper.java`：按 YYYYMMDD 精确匹配，消除中文字符串歧义

---

## 九、播放器稳定性修复（散布全天）

### 9.1 MainActivity.java（42行）

- 安卓 13+/16 手势返回双保险：`OnBackPressedCallback` + 系统 `OnBackInvokedCallback`（避免弹窗两次 / 内存泄漏）
- 频道空列表防御式播放（index 合法 + 有播放地址才 playChannel）
- 小窗/自由窗口模式调整窗口大小时保持播放不中断
- 失去窗口焦点时不暂停播放器
- 不在 onDestroy 关闭静态线程池（避免重开应用黑屏）

### 9.2 TVPlayerManager.java（113行）

- 卡顿检测：用播放器实际状态判断是否真正卡住（而非仅看 position 是否变化）
- 黑屏修复：识别 Surface 失效异常（IllegalArgumentException）、addView 后立即注册 SurfaceHolder.Callback、切前台 Surface 未就绪时强制绑定、最多重试 10 次（3秒）避免无限循环
- 虎牙解析 `variantList` 填充 + 线路轮询 + 并行加载优化

### 9.3 其他

- `WebServerManager.java`：优先直接通知主界面（比广播更可靠、更实时）
- `RedirectLoggingHttpDataSource.java`：重定向日志调整
- `LifecycleHelper.java`：不在 releaseAll 关闭静态线程池
- `InfoDisplayManager.java`：EPG 后台查询复用单线程池（避免每次切台 new Thread）
- `DisplayManager.java`：防重复触发系统窗口重绘 + 判空防崩溃
- `PlayerGestureHelper.java`：防连击锁和方向判定优化

---

## 十、性能优化（08:56 起）

### 10.1 AppExecutors.java（新文件，08:56）

**统一线程池工具类**，替代裸 `new Thread`：
- `AppExecutors.io(runnable)`：IO 线程池执行
- 消除每次操作 new Thread 的创建开销

使用位置：SettingsDialog 二维码生成等。

### 10.2 线程池复用

- `InfoDisplayManager`：EPG 后台查询复用单线程池
- `MainActivity` / `LifecycleHelper`：静态线程池不随生命周期关闭（避免重开应用黑屏/解析失败）

---

## 十一、调查中使用的方法与工具

1. **logcat 日志分析**：抓取 `variantList 填充` / `doPlay` / `playUrlInternal` / `playHuyaStream` / `parseFull` / 缓存命中 / 线路轮询日志，定位每次播放的解析路径
2. **源码追踪**：`TVPlayerManager` / `HuyaStreamPlayer` / `MainActivity` / `Channel` / `ChannelPanelController` / `VariantManager` / `HuyaSDKParser` 全链路阅读
3. **UI 实测**：adb 截图 + `input keyevent/tap` 模拟遥控器操作，打开"清晰度选择"对话框观察档位
4. **git diff 回溯**：定位今日全部改动文件（时间戳 08:56-13:57）
5. **adb 常用命令**：
   - 截图：`adb shell screencap -p /sdcard/x.png` + `adb pull`
   - 按键：`adb shell input keyevent KEYCODE_DPAD_DOWN/CENTER/BACK`
   - 日志：`adb logcat -d -v time | Select-String ...`

---

## 十二、待办事项 / 后续关注

1. ~~切频道返回后清晰度档位递减~~（已解决，用户确认）
2. **线路轮询逻辑**（TVPlayerManager 1584-1645）：多次进入同一直播间自动切换线路——功能保留，需持续观察是否影响体验（每次切回频道会轮询到下一线路）
3. **HuyaStreamPlayer 与 TVPlayerManager 双路径**：UI 实际读取哪条路径的 variantList 需保持监控（两处 setMainPlayUrl 已同步修复）
4. **APK 体积优化回归**：`useLegacyPackaging=true` + `shrinkResources=true` 后 release 构建需验证无资源缺失；`libmarsxlog.so`/`libstlport_shared.so`/`libc++_shared.so` 依赖关系已注释保留，后续混淆/剔除 SO 时务必遵守
5. **普通 m3u 多档解析**：`hs.hls.huya.com/src/...m3u8` 普通流也能解析出多档（走 parseMasterPlaylist）——此路径档位正常，无需改动

---

## 十三、今日改动文件总清单（33 个）

| 时间 | 文件 | 类别 |
|------|------|------|
| 08:56 | `util/AppExecutors.java`（新） | 性能优化 |
| 09:08 | `EpgManager.java` | EPG 修复 |
| 09:08 | `loader/LiveSourceLoader.java` | 直播源 |
| 09:08 | `manager/AppCoreManager.java` | 功能修复 |
| 09:08 | `PlaylistParser.java` | 解析重构 |
| 09:08 | `security/AntiDebug.java` | 安全 |
| 09:08 | `security/SecurityGuard.java` | 安全 |
| 09:08 | `security/TamperReporter.java` | 安全 |
| 09:08 | `SettingsDialog.java` | 直播源 UI |
| 09:08 | `SourceManager.java` | 直播源 |
| 09:08 | `SubscriptionAdapter.java` | 直播源 UI |
| 09:08 | `UrlConfig.java` | 配置 |
| 09:08 | `util/BuglyLogSender.java` | 上报 |
| 09:08 | `util/ExceptionReporter.java` | 上报 |
| 09:08 | `util/HuyaSDKLogger.java` | 日志 |
| 09:08 | `util/LogCollector.java` | 日志 |
| 09:08 | `util/LogServer.java` | 日志 |
| 09:08 | `WebServerManager.java` | 修复 |
| 09:08 | `widget/EpgManagerWrapper.java` | EPG 修复 |
| 09:08 | `res/color+drawable+layout/subscription_*`（5个） | 直播源 UI |
| 09:56 | `util/VariantManager.java` | 清晰度 |
| 09:56 | `RedirectLoggingHttpDataSource.java` | 日志 |
| 10:16 | `util/HuyaParser.java` | 虎牙解析 |
| 11:32 | `app/build.gradle` | **减小体积** |
| 12:12 | `util/SdkCompatHook.java`（新） | SDK 兼容 |
| 13:13 | `MyApplication.java` | SDK 兼容 |
| 13:24 | `util/HuyaSDKParser.java` | 虎牙解析/优化 |
| 13:40 | `util/Variant.java` | **SDK 清晰度对接** |
| 13:57 | `TVPlayerManager.java` | **清晰度残留修复** |
| 13:57 | `util/HuyaStreamPlayer.java` | **清晰度残留修复** |
| 13:57 | `MainActivity.java` | **清晰度残留修复** |
| - | 删除 `assets/js/` 三个 js | **减小体积** |
| - | `log-monitor/`（server.js/app.js 等） | 日志监控 |

---

## 十四、总结

- **核心问题**：虎牙解析后覆写 Channel.mainPlayUrl 导致切回频道不重新解析，variantList 残留上一房间档位
- **修复原则**：保持 `huya://room/xxx` 房间协议不变，切回必然重新解析；线路 URL 仍写 backupUrls 供线路选择
- **体积优化**：shrinkResources + SO deflate 压缩 + 删除 assets/js → release APK 降至 10.6MB
- **当前状态**：已编译安装，用户确认清晰度问题解决
